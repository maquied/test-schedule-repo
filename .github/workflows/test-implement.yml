name: Test-Implement
on:
  pull_request:
    types: [closed]
    branches-ignore:
        - main
#  push:
#    branches: [ "implement/nonprod-*" ]
  workflow_dispatch:
jobs:
  implement:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: Checkout Branch
        uses: actions/checkout@v3
        
      - name: Run a one-line script
        run: echo Hello, Implement!